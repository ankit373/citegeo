apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "citegeo.fullname" . }}
  labels:
    {{- include "citegeo.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{- if $value }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
  {{- end }}
