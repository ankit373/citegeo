# Commits that moved a lot of lines without changing behaviour. Ignore them in
# blame:  git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# (Add the full 40-character SHA of any future mass rename or reformat here.)
